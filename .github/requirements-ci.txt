# Pipeline toolchain, pinned so a hook bump is a reviewable PR (dependabot
# watches this file: pip ecosystem, directory /.github).
pre-commit==4.6.2
ruff==0.16.4
mypy==2.3.1
bandit==1.9.4
pytest==9.1.1
